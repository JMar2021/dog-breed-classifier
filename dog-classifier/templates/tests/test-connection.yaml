apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dog-classifier.fullname" . }}-test-connection"
  labels:
    {{- include "dog-classifier.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dog-classifier.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
